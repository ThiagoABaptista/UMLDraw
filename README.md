# 🧩 UMLDraw — Extensão para VS Code

## 🧪 Teste da Extensão UMLDraw via GitHub Codespaces

Este repositório está configurado para rodar a extensão **UMLDraw** diretamente no ambiente do **VS Code** via **Codespaces**.

## 🚀 Como testar

1. Acesse o repositório:  
   👉 [https://github.com/ThiagoABaptista/UMLDraw](https://github.com/ThiagoABaptista/UMLDraw)

2. Clique no botão verde **"Code" → "Create Codespace on main"**.

3. Aguarde o ambiente carregar (leva 1 a 2 minutos).

4. Após o carregamento:
   - O projeto será compilado automaticamente.
   - Pressione **F5** (ou `Run > Start Debugging`).
   - Uma nova janela do VS Code será aberta com a extensão UMLDraw em modo de teste.

## 💡 Dicas

- Use o comando `npm run compile` para compilar manualmente a extensão.
- Use `npm run package` se quiser gerar o `.vsix` instalável.
- Quaisquer alterações no código são refletidas ao reiniciar o modo de depuração.

---
🧠 Desenvolvido para testes e avaliação do **UMLDraw** dentro do ambiente VS Code.
